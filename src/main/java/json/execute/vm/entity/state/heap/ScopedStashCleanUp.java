package json.execute.vm.entity.state.heap;

public class ScopedStashCleanUp {

    private Heap heap;
    private long sz;
    public ScopedStashCleanUp(Heap heap) {
        this.heap = heap;
    }
}
